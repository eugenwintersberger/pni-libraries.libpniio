#makefile for building debian packages

SCONS=scons

clean:
	$(SCONS) -c all
	$(SCONS) -c python

all:
	$(SCONS) all
	$(SCONS) python

install:
	$(SCONS) PREFIX=@PREFIX@ --install-sandbox=$(DESTDIR) install CXX=$(CXX)

